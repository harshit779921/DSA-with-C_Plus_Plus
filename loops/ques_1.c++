// a program in C++ to find the first 10 natural numbers.
#include <iostream>

using namespace std;
int main()
{
    int n = 10;
    for (int i = 1; i <= n; i++)
    {
        cout << "numbers are:" << i << endl;
    }
    return 0;
}